// Simple CREATION OF Object
    let person={
        FirstName:"Pratik",
        LastName:"Kadam",
        Age:"26"};
    console.log(person.FirstName +" is "+person.Age+" years old");

    //inerpolation::: methodfor calling object
    console.log(`${person.FirstName} is  ${person.Age}  years old`);
    
    console.log("fist name is",person.FirstName);

    /// using new key word

    let car=new Object({
        type:"Suv",
        color:"red",
        fueltype:"petrol"
    })
    console.log(`the car is ${car.type} color is ${car.color}`);
    
